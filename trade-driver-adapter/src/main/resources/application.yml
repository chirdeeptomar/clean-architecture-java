spring:
  application:
    name: "trading-application"

  datasource:
    hikari:
      connection-timeout: 20000
      maximum-pool-size: 50
    url: jdbc:postgresql://localhost:32769/trading
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create

server:
  port: 8081
